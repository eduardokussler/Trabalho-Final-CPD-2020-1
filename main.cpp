#include "Shell.h"

int main() {
  Shell shell;
  shell.readFiles();
  //std::vector<std::string> taags = {"Quentin Tarantino"};
  //shell.tags(taags);
  //shell.topGenre(5,"AcTiOn");
  //shell.user(4);
  shell.readShell();

  return 0;
}